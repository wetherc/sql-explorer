{
  "name": "sql-explorer-monorepo",
  "private": true,
  "scripts": {
    "bootstrap": "pnpm install",
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "test": "pnpm --filter frontend test:unit && cd backend && cargo test",
    "test:unit": "pnpm --filter frontend test:unit",
    "test:coverage": "pnpm --filter frontend test:coverage",
    "lint": "pnpm --filter frontend lint && cd backend && cargo clippy --all-targets -- -D warnings && cargo fmt --check",
    "format": "pnpm --filter frontend format && cd backend && cargo fmt",
    "verify": "pnpm lint && pnpm test",
    "prepare": "git config core.hooksPath .githooks || true"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
