{
  "name": "sql-explorer-monorepo",
  "private": true,
  "scripts": {
    "bootstrap": "pnpm install",
    "dev": "cd backend && tauri dev",
    "build": "cd backend && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.9"
  }
}
